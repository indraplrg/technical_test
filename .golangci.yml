version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - gofmt
    - goimports
    - errcheck
    - staticcheck
    - ineffassign
    - unused
    - revive
    - gosec

issues:
  max-issues-per-linter: 0
  max-same-issues: 0